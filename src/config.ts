/**
 * Application configuration
 */

export const config = {
  /**
   * Affiliate mode - appends ?affid=<brandId> to external product URLs
   */
  AFFILIATE_MODE: true,

  /**
   * App metadata
   */
  APP_NAME: 'AesthetIQ',
  APP_URL: 'https://aesthetiq.app',
} as const;
